{
  "name": "tsonic.org",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "node --test \"test/**/*.test.mjs\"",
    "check": "npm run build && npm test"
  }
}
